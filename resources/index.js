const projectimg = document.getElementsByClassName("projectimg");

function zoom (projectimg) {
    projectimg.style.transform = 'scale(1.5)';
};

function rezoom (projectimg) {
    projectimg.style.transform = 'scale(1)';
}